package ru.mobnius.cic.adaper.holder.phototypes;

/**
 * Обработчик возврата позиции выбранного типа фото в адаптер
 */
public interface ImageTypeCheckedPositionCallback {
    void onPositionChecked(final int checkedPosition);
}
